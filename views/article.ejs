<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Trading Articles & Stock Insights</title>
  <style>
    /* General Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Poppins', sans-serif;
      scroll-behavior: smooth;
    }

    /* Body Styling */
    body {
      background: linear-gradient(to bottom right, #0f2027, #203a43, #2c5364);
      color: #fff;
      text-align: center;
      min-height: 100vh;
    }

    /* Header Section */
    header {
      background: rgba(0, 0, 0, 0.5);
      padding: 20px 0;
      border-radius: 8px;
      margin: 20px auto;
      max-width: 900px;
      animation: fadeInDown 1s ease-in-out;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
    }

    header h1 {
      font-size: 2.8rem;
      letter-spacing: 1px;
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
    }

    /* Fade-in Animation */
    @keyframes fadeInDown {
      0% {
        opacity: 0;
        transform: translateY(-20px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Article Container */
    .articles-container {
      background: #f9f9f9;
      color: #333;
      border-radius: 12px;
      margin: 30px auto;
      max-width: 850px;
      padding: 30px;
      box-shadow: 0 6px 15px rgba(0, 0, 0, 0.2);
      animation: slideIn 1s ease-in-out;
    }

    .articles-container h2 {
      font-size: 2rem;
      color: #1e3c72;
      margin-bottom: 20px;
      text-align: center;
    }

    @keyframes slideIn {
      0% {
        opacity: 0;
        transform: translateY(50px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Article List */
    .article-list {
      list-style: none;
      text-align: left;
    }

    .article-list li {
      padding: 15px;
      border-bottom: 1px solid #ddd;
      transition: all 0.3s ease;
    }

    .article-list li:hover {
      background: #e8f4ff;
      border-left: 5px solid #2980b9;
      transform: translateX(8px);
    }

    .article-list a {
      text-decoration: none;
      color: #333;
      font-weight: bold;
      display: block;
    }

    .article-list a:hover {
      color: #2980b9;
    }

    /* Instruction Section */
    .instructions {
      background: #203a43;
      margin: 40px auto;
      border-radius: 12px;
      padding: 25px;
      max-width: 850px;
      color: #f2f2f2;
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.3);
      animation: zoomIn 1s ease-in-out;
    }

    .instructions h3 {
      margin-bottom: 20px;
      font-size: 1.8rem;
      color: #f2f2f2;
    }

    .instructions ul {
      text-align: left;
      list-style-type: decimal;
      padding-left: 20px;
    }

    .instructions li {
      margin: 10px 0;
      font-size: 1.1rem;
    }

    @keyframes zoomIn {
      0% {
        transform: scale(0.8);
        opacity: 0;
      }
      100% {
        transform: scale(1);
        opacity: 1;
      }
    }

    /* Buttons */
    .button-container {
      margin: 30px auto;
      display: flex;
      justify-content: center;
      gap: 20px;
      flex-wrap: wrap;
    }

    button {
      background: linear-gradient(to right, #27ae60, #2ecc71);
      color: #fff;
      border: none;
      border-radius: 50px;
      padding: 12px 24px;
      font-size: 1.2rem;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.4);
    }

    button:hover {
      background: linear-gradient(to right, #2ecc71, #27ae60);
      transform: translateY(-5px) scale(1.05);
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.6);
    }

    /* Footer */
    footer {
      margin-top: 50px;
      font-size: 0.9rem;
      color: #ddd;
      text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
    }
  </style>
</head>
<body>
  <!-- Header -->
  <header>
    <h1>Explore Trading Articles & Stock Insights</h1>
  </header>

  <!-- Articles Section -->
  <section class="articles-container">
    <h2>Latest Articles on Trading</h2>
    <ul class="article-list">
      <li><a href="#">How to Start Trading: Beginner's Guide</a></li>
      <li><a href="#">Top 10 Tips for Intraday Trading</a></li>
      <li><a href="#">Understanding the NSE and BSE Stock Markets</a></li>
      <li><a href="#">Best Strategies for Long-Term Investments</a></li>
      <li><a href="#">How to Analyze Stock Trends and Patterns</a></li>
    </ul>
  </section>

  <!-- Instructions Section -->
  <section class="instructions">
    <h3>How to Use This Page</h3>
    <ul>
      <li>Explore the articles to learn the basics and advanced strategies for trading.</li>
      <li>Click the buttons below to view the latest top-performing NSE and BSE stocks.</li>
      <li>Stay updated and analyze stock patterns to improve your trading knowledge.</li>
    </ul>
  </section>

  <!-- Navigation Buttons -->
  <div class="button-container">
    <button onclick="location.href='/most-active-stocksnse'">View Top 5 NSE Stocks</button>
    <button onclick="location.href='/most-active-stocksbse'">View Top 5 BSE Stocks</button>
  </div>

  <!-- Footer -->
  <footer>
    <p>© 2024 Trading Insights. All rights reserved.</p>
  </footer>
</body>
</html>
